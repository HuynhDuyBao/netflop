{
  "name": "netflop",
  "version": "1.0.0",
  "private": true,
  "description": "Backend API for the Netflop movie streaming app.",
  "scripts": {
    "db:migrate": "npm --prefix server run db:migrate",
    "db:seed": "npm --prefix server run db:seed",
    "dev": "npm --prefix server run dev",
    "start": "npm --prefix server start"
  },
  "engines": {
    "node": ">=18"
  }
}
